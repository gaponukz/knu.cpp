#include <iostream>
#include <vector>

class Tensor {
    public:

    uint32_t = number_dim;
    uint32_t* shapes;
    uint32_t* data;

    Tensor() {
        // std::memcpy(m_data,data,rows*cols*sizeof(float));
    }

};

const bool is_pzdz = 1;